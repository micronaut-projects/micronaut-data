micronaut:
  http:
    client:
      read-timeout: 5m
datasources:
  default:
    db-type: oracle
    dialect: ORACLE
r2dbc:
  datasources:
    default:
      db-type: oracle
      dialect: ORACLE
      schema-generate: CREATE_DROP
test-resources:
  containers:
    oracle:
      startup-timeout: 600s
      db-name: test
