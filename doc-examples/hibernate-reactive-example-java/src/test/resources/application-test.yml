jpa:
  default:
    properties:
      hibernate:
        id:
          db_structure_naming_strategy: "legacy"
        connection:
          db-type: "mysql"
        show_sql: true
test-resources:
  containers:
    mysql:
      image-name: "mysql:8.0.30"
