micronaut:
  application:
    name: jdbc-sessionless-transaction-booking-java
  data:
    oracle:
      sessionless:
        http:
          propagation-enabled: true

datasources:
  default:
    db-type: oracle
    dialect: ORACLE
    schema-generate: CREATE_DROP
