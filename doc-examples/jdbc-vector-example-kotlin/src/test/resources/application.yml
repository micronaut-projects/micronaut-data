test-resources:
  containers:
    oracle:
      startup-timeout: 600s
      db-name: test

datasources:
  default:
    db-type: oracle
    schema-generate: CREATE_DROP
    dialect: ORACLE

micronaut:
  http:
    client:
      read-timeout: 5m
