micronaut:
  application:
    name: example
  propagation: thread_local

r2dbc:
  datasources:
    default:
      db-type: postgres
      dialect: postgres
datasources:
  default:
    db-type: postgres
    dialect: postgres
