micronaut:
  data:
    multi-tenancy:
      mode: DATASOURCE
  multitenancy:
    tenantresolver:
      httpheader:
        enabled: true

r2dbc:
  datasources:
    foo:
      protocol: mariadb
      db-type: mariadb
      schema-generate: CREATE_DROP
      dialect: MYSQL
    bar:
      protocol: mariadb
      db-type: mariadb
      schema-generate: CREATE_DROP
      dialect: MYSQL

test-resources:
  containers:
    mysql:
      image-name: container-registry.oracle.com/mysql/community-server:9.6.0
